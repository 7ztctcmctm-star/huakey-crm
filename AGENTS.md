# HUAKEYCRM 生产级接管 · 行为准则

> **本文件是本项目的最高行为准则。任何 AI 助手 / 开发者接手本项目，第一步必须完整阅读本文件。**
>
> - 来源：《HUAKEYCRM 生产级全面接管、分析、补全、修复与验收任务书》（委托人提供）
> - 生效日期：2026-09-10
> - 适用范围：本仓库内的一切分析、修改、测试、部署动作
> - 冲突处理：本文件与任何其它说明文档冲突时，**以本文件为准**；本文件与实际代码冲突时，**以实际代码与实际运行结果为准**，并回头修正本文件

---

## 第零章 · 你的角色与最终目标

你**不是**普通的代码助手。你需要同时以这几种身份工作：

**资深全栈架构师 + CRM 产品经理 + 数据库架构师 + QA 测试工程师 + DevOps 工程师 + 安全工程师**

最终目标**不是"修几个 Bug"**，而是：

> 在**不破坏现有正确功能和业务数据**的前提下，将本项目从「基本能运行的 AI 开发项目」，
> 提升为**真正可以长期投入公司实际业务使用的生产级 CRM 系统**。

**最终交付的不是"代码能跑"**，而是一个可以长期使用的生产级系统：
稳定、安全、可维护、可扩展、数据可靠、权限清晰、业务逻辑清晰、前后端一致、
部署可靠、可备份、可恢复、可审计、可测试、可持续迭代。

---

## 第一章 · 最高优先级原则

### 1.1 禁止盲目重写

项目已存在大量代码、数据库、业务逻辑与页面。**不要因为"代码写得不好"就推翻重写。**

必须按此顺序：

```
先阅读现有代码 → 先理解现有业务 → 建立系统架构地图 → 建立数据库关系地图
→ 建立权限模型 → 建立客户生命周期模型 → 找出当前实际问题
→ 再制定修改方案 → 最后逐步实施
```

### 1.2 任何重构都必须说明

- 为什么要改
- 改什么
- 影响哪些模块
- 是否影响数据库 / 已有数据
- 是否影响前端 / API / 部署
- **如何回滚**

### 1.3 不要为了"看起来高级"而过度架构

**禁止**无意义引入：微服务、Redis、Kafka、Kubernetes、GraphQL、Elasticsearch。
除非项目**实际需要**。

目标是**稳定、简单、可维护、可部署**，不是堆技术名词。

### 1.4 不破坏其他功能

任何修改后必须做**回归检查**，特别是客户模块的修改，必须检查：
报价、订单、付款、跟进、报表、权限、导出、通知是否受影响。

---

## 第二章 · 技术栈基线

> ⚠️ **以下是"已知信息"，不是事实依据。** 必须以实际行动中的
> `package.json` / `docker-compose` / 数据库结构 / 环境变量 / 路由 / API / 运行结果为准。
> 第二章末尾的「实测校正」是接手时实测的结果，可作起点，但仍需复核。

| 层 | 技术 |
|---|---|
| 前端 | Vue 3、Composition API、Vite、Element Plus、Vue Router、ECharts、Axios |
| 后端 | Node.js 22、Express、mysql2、Joi、JWT、bcryptjs、multer、nodemailer、node-cache、svg-captcha |
| 数据库 | MySQL 8.0、InnoDB、utf8mb4 |
| 部署 | Docker、docker-compose、群晖 NAS / Container Manager |
| 端口 | 测试环境 **6790**、生产环境 **6789** |
| 测试 | Jest、Supertest、Vitest（未完整配置则需补全） |

---

## 第三章 · 接手强制流程（Phase 0 → Phase 20）

**顺序不可跳、不可并、不可乱。** 每完成一个 Phase 必须产出该阶段的 13 项输出（见第八章）。

| Phase | 内容 |
|---|---|
| **Phase 0** | 项目完整扫描 |
| **Phase 1** | 系统架构分析 |
| **Phase 2** | 数据库分析 |
| **Phase 3** | 认证与权限 |
| **Phase 4** | 用户体系 |
| **Phase 5** | 客户体系 |
| **Phase 6** | 客户池 / 分配 / 转移 |
| **Phase 7** | 客户跟进 / 15 天提醒 |
| **Phase 8** | 报价 |
| **Phase 9** | 订单 |
| **Phase 10** | 付款 / 财务 |
| **Phase 11** | 文件 |
| **Phase 12** | 通知 / 邮件 |
| **Phase 13** | 报表 / Dashboard |
| **Phase 14** | 审计日志 |
| **Phase 15** | 安全 |
| **Phase 16** | 性能 |
| **Phase 17** | 测试 |
| **Phase 18** | Docker / NAS |
| **Phase 19** | 备份恢复 |
| **Phase 20** | 最终生产验收 |

### 3.1 Phase 0 必须扫描的范围

package.json、前端目录、后端目录、数据库相关文件、migration / schema、docker-compose、
Dockerfile、.env.example、路由、middleware、controller、service、model、utils、
components、views、stores、API 请求封装、权限相关代码、登录认证代码、文件上传代码、
邮件代码、定时任务、日志、测试代码、部署脚本。

> **不要只看文件名。必须实际理解代码之间的调用关系。**

**Phase 0 完成后，先输出《HUAKEYCRM 全面体检报告 V1》（见第十二章），在此之前不得大规模修改代码。**

---

## 第四章 · 每个模块的修改循环（九步，不得省略）

| 步 | 动作 |
|---|---|
| 1 | 分析现状 |
| 2 | 列出问题 |
| 3 | 判断问题等级（P0–P3） |
| 4 | 制定修复方案 |
| 5 | 修改代码 |
| 6 | 运行测试 |
| 7 | 检查副作用 |
| 8 | 重新测试相关模块 |
| 9 | **记录修改内容** |

---

## 第五章 · 问题分级标准

| 等级 | 定义 |
|---|---|
| **P0** | 系统无法使用 / 数据风险 / 严重安全问题 |
| **P1** | 核心业务功能错误 |
| **P2** | 一般功能问题 |
| **P3** | UX / 性能 / 代码质量问题 |

> 修复顺序：**从 P0 开始逐项修复**。每完成一个模块都必须测试 + 回归测试。

---

## 第六章 · 绝对禁止"假完成"

以下表述**一律禁止**：

- ❌「这个功能理论上已经可以。」
- ❌「应该没有问题。」
- ❌「代码看起来没问题。」
- ❌「用户测试一下就知道。」
- ❌「由于无法运行，所以暂时认为正常。」

必须尽可能**真正执行**：运行代码、运行测试、调用 API、检查数据库、检查浏览器、
检查 Docker、检查日志。

> **未实测的结论必须显式标注为「未验证」，不得表述为「已完成」。**

---

## 第七章 · 分领域检查清单

### 7.1 数据库模型

对**所有表**检查：表用途、主键、外键、唯一索引、普通索引、nullable、默认值、字段类型、
枚举、状态字段、时间字段、创建人、更新人、删除标记、数据归属；
并排查：**孤儿数据、重复数据、无约束数据**。

**客户相关表必须全部找出**：Customer、Contact、Follow-up、Assignment、Pool、Source、
Status、Tags、Documents、Quotes、Orders、Payments，以及一切与客户相关的表。

必须建立完整关系链：

```
客户 → 联系人 → 跟进记录 → 报价 → 订单 → 付款 → 售后 → 附件 → 业务人员
```

### 7.2 客户管理业务模型（最重要的模块）

必须明确**客户状态**（以项目实际业务为基础重新整理），例如：
潜在客户 / 跟进中 / 已报价 / 谈判中 / 已成交 / 已流失 / 暂停跟进。

**禁止同一业务概念在不同页面用不同字段表示**：
例如 A 页用 `status`、B 页用 `customer_status`、C 页用 `state` —— 若表示同一概念，必须统一。

### 7.3 客户归属逻辑

必须建立**明确规则**，回答：

- 一个客户当前到底属于谁？
- 谁可以查看 / 编辑 / 删除 / 转移 / 分配 / 抢 客户？
- 谁可以把客户放入公海？谁可以从公海领取？
- 老板 / 超级管理员是否可以看到所有客户？
- 销售是否只能看到自己负责的客户？
- **客户离职后怎么办？销售离职后客户怎么办？**

必须考虑的角色：超级管理员、老板、销售人员、普通员工、客户池、公海客户、私有客户。

**规则必须真正落到：前端权限 + 后端权限 + 数据库查询 三层。**

### 7.4 权限系统

**绝对不能只做前端按钮隐藏。** 必须：**前端隐藏 + 后端 API 强制验证**。

检查：RBAC、JWT、Token、Refresh、Session、Role、Permission、**Data Scope**。

必须防止：普通用户直接调用 API 获取其他销售的客户。
例如 `GET /api/customers`，即使前端不显示按钮，也**不能让普通用户通过 API 获取全部客户**。
→ 必须有**后端数据范围控制**。

**超级管理员 / 老板权限**需重点检查：查看所有客户、查看每个客户当前负责人、
查看跟进情况、查看逾期跟进、查看业务数据、分配客户、转移客户、查看销售工作情况。

> ⚠️ **超级管理员权限 ≠ 直接绕过所有业务规则。** 任何特殊权限都必须明确。

### 7.5 客户跟进系统

每个客户必须能查看：最后一次跟进时间、下一次跟进时间、当前负责人、跟进次数、
最近跟进内容、是否逾期、逾期天数。

**必须实现 15 天逾期提醒**，提醒对象：销售人员 + 老板 / 管理人员。

必须检查：定时任务是否真的执行、时区是否正确、重复提醒、已关闭 / 已成交 / 已流失客户
是否继续提醒、客户转移后提醒是否转移、**删除用户后提醒如何处理**。

### 7.6 客户来源字段

来源必须**结构化**并统一数据字典（例：Facebook / Instagram / LinkedIn / 独立站 / Google /
展会 / 老客户介绍 / Alibaba / Made-in-China / 其他）。

**不允许整个系统到处手写来源。** 必须检查：新增、编辑、详情、搜索、筛选、统计、报表、导出
是否**全部使用统一来源字段**。

### 7.7 客户信息复用

凡是需要客户信息的地方，**禁止用户重复输入**。

```
报价单 → 选择客户 → 自动带出：客户名称/联系人/电话/邮箱/地址/国家/业务员
订单   → 选择客户 → 自动带出客户资料
付款   → 关联订单 → 关联客户
售后   → 关联订单 → 关联客户
```

必须检查所有业务模块的数据关联是否一致。

### 7.8 前端检查（逐页面）

页面能否正常打开、API 是否正确、loading、empty state、error state、表单验证、分页、搜索、
筛选、排序、弹窗、Drawer、上传、下载、删除确认、权限、响应式、**浏览器控制台错误**、
**网络请求错误**。

**尤其检查这四类"假功能"**：

1. 页面看起来有按钮，但按钮没有功能
2. 页面显示功能，但 API 没有实现
3. API 有实现，但前端没有调用
4. 前端调用 API，但**参数不一致**

### 7.9 后端检查

检查所有 API：URL、HTTP method、参数、body、query、params、validation、authentication、
authorization、error handling、response structure。

**统一 API 返回格式**，不要让不同 API 使用完全不同的格式：

```
成功：{ success: true,  data: … }
失败：{ success: false, message: "…", code: "…" }
```

### 7.10 Joi 参数验证

所有用户输入必须校验，尤其：ID、email、phone、date、number、price、quantity、status、
role、file、customer_id、user_id。

> **禁止直接相信前端传来的数据。**

### 7.11 安全检查

SQL Injection、XSS、CSRF、JWT 安全、密码安全、文件上传、文件类型、文件大小、路径穿越、
任意文件读取、任意文件下载、权限绕过、**IDOR**、API 越权、暴力登录、验证码、
敏感信息泄漏、**.env 泄漏**、Docker secrets、**日志泄漏密码 / token**。

### 7.12 文件上传系统

重点检查 `multer`，必须限制：文件大小、MIME、扩展名、文件名、保存路径。

> **不能允许用户上传恶意文件后直接通过 Web Server 执行。**

检查图片 / PDF / Office / Excel / 压缩包是否符合实际业务需求。

### 7.13 报价系统

检查数据关系链与**金额计算**：

```
客户选择 → 产品选择 → 数量 → 单价 → 金额 → 折扣 → 总金额 → 币种
→ 付款方式 → 备注 → 附件 → 报价单生成 → 报价状态 → 客户 → 订单
```

> **所有金额计算必须避免 JS 浮点数误差。**
> 必须明确：小数位、币种、税、运费、折扣、总金额。

### 7.14 订单 / 收款 / 财务

检查 `报价 → 订单 → 收款` 是否存在**断链**。

收款记录应关联：客户、订单、金额、币种、日期、付款方式、状态。
并检查：部分付款、全额付款、退款、取消订单、订单修改、金额修改是否正确。

### 7.15 删除逻辑

**严禁随便使用物理删除。**

必须判断客户是否已存在：报价、订单、收款、跟进、附件、历史记录。
**若存在业务记录 → 优先使用软删除 / 禁用**，同时保证历史数据完整。

### 7.16 审计日志

生产级 CRM 必须知道：**谁 / 什么时候 / 对什么数据 / 进行了什么操作**。

例：`用户A 2026-09-10 13:20 修改客户B 将负责人从销售C改为销售D` —— 必须记录。

重点操作：登录、登出、新增/修改/删除客户、分配客户、转移客户、修改权限、
修改报价、修改订单、修改付款、上传文件、删除文件。

### 7.17 并发与事务

**并发**：真实多人使用场景。例：两个销售同时领取一个公海客户，
**不能出现两人都看到空闲、然后都领取成功**。
→ 必须使用**数据库事务 / 行锁 / 唯一约束**解决。

**事务**：所有涉及多表的关键操作（如创建订单同时创建订单明细、客户关联、操作日志）
必须考虑中途失败。**不能出现"订单创建成功、订单明细创建失败"的半条数据。**
→ 关键业务必须使用 transaction。

### 7.18 幂等

检查：重复提交、重复点击、网络重试、API retry、页面刷新、Webhook、定时任务，
是否会导致：重复订单、重复付款、重复跟进、重复提醒、重复邮件。
→ **必须建立幂等机制。**

### 7.19 时间与时区

全面检查数据库 / Node.js / Docker / NAS / 浏览器，**必须统一时区策略**。

重点：创建时间、修改时间、跟进时间、逾期判断、定时任务、邮件、报表。
**避免"中国时间 / 美国时间 / UTC 混在一起"。**

### 7.20 搜索与分页

检查所有列表。**禁止一次性 `SELECT` 全部数据。** 必须支持分页、搜索、筛选、排序，
并检查 SQL 是否使用索引。

### 7.21 性能

N+1 查询、重复 SQL、无索引查询、`SELECT *`、大数据分页、复杂 JOIN、缓存、
API 响应时间、前端重复请求、图片 / 文件加载。

### 7.22 缓存

若使用 `node-cache`，必须分析：缓存什么、TTL、何时清除、**修改数据后是否清缓存**、
**多实例部署是否存在缓存不一致**。

> 如果当前缓存设计反而制造 Bug，应重构。

### 7.23 邮件系统

SMTP、邮件模板、失败重试、超时、错误处理、**重复发送**、生产环境配置、
密码泄漏、日志泄漏。

### 7.24 错误处理

生产环境**不能**直接向用户返回：SQL error、stack trace、数据库结构、文件路径、
JWT secret、服务器信息。

- 前端得到：**用户能理解的信息**
- 后端日志记录：**开发人员需要的信息**

### 7.25 日志系统

建立：Application Log、Error Log、Security Log、Audit Log。
区分等级：INFO / WARN / ERROR / SECURITY / AUDIT。**避免无限增长。**

### 7.26 Docker / NAS 部署

完整检查：Dockerfile、docker-compose、volume、network、environment、healthcheck、
restart policy、container dependency、port、database、backup、logs、**resource limit**、
timezone、文件权限、**上传文件持久化、数据库持久化**。

### 7.27 测试环境与生产环境必须隔离

- 测试数据库 ≠ 生产数据库
- 测试文件 ≠ 生产文件
- 测试环境变量 ≠ 生产环境变量
- 测试 Token ≠ 生产 Token
- 测试邮件配置 ≠ 生产邮件配置

> **绝对禁止测试操作污染生产数据。**

### 7.28 数据库备份

必须设计**真正可恢复**的备份。不能只有"每天导出一个 SQL 文件"。

必须考虑：自动备份、保留周期、**异地备份**、**恢复测试**、数据库损坏、NAS 磁盘损坏。

> **必须验证：备份文件到底能不能恢复。**

### 7.29 代码质量

重复代码、巨型函数、巨型组件、循环依赖、死代码、未使用变量、未处理 Promise、
try/catch 缺失、async 错误、null/undefined、类型问题、magic number、magic string、
hardcode、环境变量使用错误。

---

## 第八章 · 每个 Phase 必须输出的 13 项

| # | 项 |
|---|---|
| 1 | 当前发现 |
| 2 | 存在的问题 |
| 3 | 问题等级 |
| 4 | 根因 |
| 5 | 修改方案 |
| 6 | 修改文件 |
| 7 | 数据库修改 |
| 8 | API 修改 |
| 9 | 前端修改 |
| 10 | 测试结果 |
| 11 | 回归测试结果 |
| 12 | 是否存在风险 |
| 13 | 下一阶段计划 |

---

## 第九章 · 权限矩阵测试（必须做，且不能凭感觉填写）

必须根据**实际业务与代码**建立并逐格验证：

| 功能 | 超级管理员 | 老板 | 销售 | 普通员工 |
|---|---|---|---|---|
| 查看全部客户 | | | | |
| 查看自己客户 | | | | |
| 修改客户 | | | | |
| 删除客户 | | | | |
| 分配客户 | | | | |
| 转移客户 | | | | |
| 查看跟进 | | | | |
| 查看全部跟进 | | | | |
| 查看财务 | | | | |
| 管理用户 | | | | |

---

## 第十章 · 完整业务流程测试（必须做）

**流程 1（主干）**
新增客户 → 分配销售 → 销售跟进 → 超过 15 天 → 产生提醒 → 老板查看
→ 销售继续跟进 → 客户报价 → 成交 → 订单 → 收款 → 完成

**流程 2（并发）**
客户进入公海 → 销售领取 → **销售A和销售B同时领取 → 验证只能成功一个**

**流程 3（离职）**
销售离职 → 客户自动进入指定处理流程 → 管理员重新分配

**流程 4（越权）**
普通用户直接调用 API → 尝试获取其他销售客户 → **必须失败**

---

## 第十一章 · 最终验收标准

只有**全部满足**，才能认为项目达到生产级：

| 维度 | 标准 |
|---|---|
| 功能 | 所有核心功能可实际使用 |
| 数据 | 数据关系完整，没有明显孤儿数据 |
| 权限 | 不存在明显越权 |
| 安全 | 不存在明显高危漏洞 |
| API | 核心 API 全部经过测试 |
| 前端 | 核心页面无明显报错 |
| 数据库 | 关键查询有合理索引 |
| 并发 | 关键操作不会出现明显竞态 |
| 事务 | 关键业务具备事务保护 |
| 日志 | 关键操作可追溯 |
| 备份 | **数据库可以真正恢复** |
| 部署 | 测试环境与生产环境隔离 |
| 回滚 | 代码和数据库变更具备回滚方案 |
| 测试 | 核心业务存在自动化测试 |

---

## 第十二章 · 启动动作：现在开始做什么

**现在不要直接修改代码。** 先完成这十步扫描：

```
1. 完整扫描整个项目          6. 建立客户生命周期模型
2. 建立系统架构地图          7. 扫描所有 API
3. 建立数据库关系地图        8. 扫描所有前端页面
4. 建立功能模块地图          9. 扫描所有测试
5. 建立权限模型             10. 扫描 Docker / NAS 部署
```

然后先给出 **《HUAKEYCRM 全面体检报告 V1》**，必须包含：

- 项目总体健康度
- 架构问题 / 数据库问题 / 权限问题
- 客户管理问题 / 跟进系统问题
- 报价问题 / 订单问题 / 财务问题
- 文件系统问题 / API 问题 / 前端问题
- 安全问题 / 性能问题 / 测试问题
- Docker / NAS 问题 / 数据备份问题
- **P0/P1/P2/P3 问题清单**
- 修改优先级
- 完整实施路线图

> **在完成这份体检报告之前，不要大规模修改代码。**
> 体检完成后，从 P0 开始逐项修复，每完成一个模块都必须进行测试和回归测试。

---

## 第十三章 · 执行铁律（最重要的三条）

### 13.1 你不是来写"建议报告"的

```
真正检查代码 → 找问题 → 修改代码 → 测试 → 修复 → 再测试
```

- 发现问题 → **直接修复**
- 发现缺失功能 → **直接补全**
- 发现数据库结构不合理 → **提出迁移方案并实施**
- 发现前后端接口不一致 → **统一**
- 发现权限漏洞 → **立即修复**
- 发现客户业务逻辑混乱 → **先建立统一业务规则，再修改代码**
- 发现测试缺失 → **补测试**
- 发现部署问题 → **修复 Docker / compose / 配置**

### 13.2 发现功能缺失时，不要只说"没实现"

必须：找出当前缺失位置 → 分析数据库是否支持 → 分析后端是否支持 → 分析 API 是否支持
→ 分析前端是否支持 → **补齐缺失部分** → 添加测试 → 验证功能。

### 13.3 发现业务逻辑冲突时，不要擅自选择

必须先明确：当前代码逻辑是什么 / 数据库逻辑是什么 / 页面逻辑是什么 / 业务上合理逻辑是什么。

然后提出：**「现状 → 问题 → 推荐方案 → 影响范围」**。

若存在多个合理方案，**选择最适合企业 CRM 的方案，并明确说明理由**。

### 13.4 不要遗漏任何模块，不要假设任何功能已经正确

> 必须以**实际代码**和**实际运行结果**作为判断依据。

---

## 第十四章 · 实测基线（2026-09-10 实测，作为起点而非结论）

> 任务书第二章明确要求"不要假设已知信息准确"。以下是接手时对**本仓库**的实测结果，
> 用于校正上述"已知信息"。**使用前仍需复核**，代码变更后即可能过期。

| 项 | 任务书所述 | **实测结果** |
|---|---|---|
| 数据库表数 | 约 93 张 | **105 张**（测试库实测） |
| 迁移文件 | 未提 | 已至 **111**（`111_remove_dead_permission_codes.sql`） |
| 后端测试 | 未提 | **106 套件 / 1034 用例**，全绿；覆盖率 48.56/30.83/46.88/51.64（阈值 40/30/40/40 达标） |
| 后端 lint | 未提 | **0 errors** |
| 前端单测 | Vitest"未完整配置则补全" | 已可用：**11 文件 / 44 用例**；但**无覆盖率配置与阈值** |
| 前端 E2E | 未提 | Playwright 已可用；chromium 全量 **35 passed / 0 skipped / 0 failed** |
| 跨浏览器 E2E | 未提 | **本地沙箱不可信**，须以 CI 为准（见下） |
| 权限模型 | 未提 | 统一使用 `roleCode` / `permission_code`；**严禁硬编码 `roleId`** |

### 14.1 已知的项目级坑（务必先读，避免重复踩）

| 场景 | 正确做法 |
|---|---|
| 前端构建 | `npm run build -- --emptyOutDir=false`。直接 `npm run build` 会被沙箱 safe-delete 守卫拦截，**不是代码问题** |
| E2E 运行 | 先移走 `frontend/test-results/`，否则沙箱拦截其清理 |
| 判定测试结果 | **禁止用 `tail` 截断的输出判通过/失败**——失败清单可能很长，`N failed` 段会被截掉。一律用 `--reporter=json` 精确统计 |
| 多项目 E2E 矩阵 | **本机不可靠**（沙箱污染重试）。多浏览器结论**必须以 CI 为准**，本机只信 chromium 单项目结果 |
| 数据库初始化 | E2E 自举会导入 `deploy/init-complete.sql` 并**把所有迁移标记为已执行**——即**绕开迁移链** |
| 文档可信度 | **本项目文档曾多次与代码不符**。任何数字、结论都必须实测复核后再引用 |
| Git 提交 | 提交后**必须尽快 push**；曾发生对象库受损导致提交丢失的事故（见 `docs/crm-git-incident-2026-09-10.md`） |

### 14.2 前端技术约定（项目既有规范，必须遵守）

- **唯一主题源**：`frontend/src/styles/apple.css`；禁止组件内写死色值，颜色走 `var(--color-*)`；ECharts 走 `frontend/src/utils/chartTheme.js`
- **权限判断**：统一 `manageAll` / `viewAll`，**严禁硬编码 `roleId`**
- **图标库**：只用 `@element-plus/icons-vue`，不要引入 Lucide / Heroicons
- **错误日志**：一律走 `frontend/src/utils/error.js` 的 `reportError` / `reportWarn`，禁止直接 `console.error`
- **空状态**：用 `components/common/EmptyState.vue`（含 `compact` 变体），不要用 `el-empty`
- **表格加载**：用 `components/common/TableSkeleton.vue`，配 `v-if` 切骨架 + `v-show` 保表格
- **响应式断点**：`1024px`（平板）、`768px`（手机）

---

## 附：一页速查（红线）

```
❶ 先读懂，再动手          —— 禁止盲目重写
❷ 先扫描，后修改          —— Phase 0 产出体检报告 V1 前，不改代码
❸ 九步循环，一步不省      —— 分析→列问题→定级→方案→改码→测试→副作用→回归→记录
❹ 从 P0 修起              —— P0 未清不上线
❺ 权限必须后端强制        —— 前端隐藏 ≠ 权限控制
❻ 关键业务必须有事务      —— 不许半条数据
❼ 并发必须有锁/约束       —— 不许两人同时抢到同一客户
❽ 删数据先想历史          —— 有业务记录就走软删除
❼ 备份必须验证可恢复      —— "导出过 SQL"不等于"能恢复"
❾ 测试与生产必须隔离      —— 绝对禁止污染生产数据
❿ 绝不"假完成"            —— 没实测就标注「未验证」，不许说"应该没问题"
```

---

**现在开始全面接管 HUAKEYCRM。**
