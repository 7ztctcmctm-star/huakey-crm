# Huakey CRM 全面审查报告

基于前后端完整审计，从企业真实使用、销售工作流、销售效率、客户管理效率、跟进效率、数据统计、移动端使用角度审查。

每个功能均经过5项评估：
1. 是否真的必要
2. 是否适合当前阶段
3. 是否会增加复杂度
4. 是否影响稳定性
5. 有没有更简单方案

---

# 当前缺失功能

## P0 - 直接影响销售干活

### 1. 客户详情的"商机"和"合同"Tab是空壳 `已修复`

客户详情页有5个Tab，其中"商机"和"合同"显示 `<el-empty>`，未接入数据。销售在客户详情页无法看到该客户关联的商机和合同，必须跳转到独立页面手动搜索。

- 必要：**是**。这是CRM核心联动，销售每天要查看
- 适合当前阶段：**是**。后端API已存在（opportunity/list 和 contract/list 支持 customer_name 筛选），只需前端接入
- 增加复杂度：**低**。在现有Tab中嵌入列表组件，复用已有API
- 影响稳定性：**无**。纯前端展示层修改
- 更简单方案：直接在Tab内调用已有 list API，传入 customer_id 筛选

### 2. 全系统无导出功能 `已修复(客户+合同)`

客户、合同、报价、商机、报表——没有任何页面支持Excel/CSV导出。企业使用CRM必须能导出数据做汇报、对账、交接。

- 必要：**是**。企业刚需，没有导出销售主管无法做周报
- 适合当前阶段：**是**。系统已有列表查询和筛选，导出是自然延伸
- 增加复杂度：**中**。后端需新增导出接口，前端加导出按钮
- 影响稳定性：**低**。导出是只读操作
- 更简单方案：先做后端 CSV 流式导出（不依赖前端），优先做客户和合同两个模块

### 3. 跟进模块功能严重不足 `已修复(编辑/删除)`

跟进记录只能新增，不能修改、不能删除。没有跟进计划视图、没有按时间线查看所有客户的跟进、没有跟进统计（每人每天跟进了多少客户）。

- 必要：**是**。跟进是CRM的灵魂，销售每天的核心动作
- 适合当前阶段：**是**。已有基础跟进功能，只需补全
- 增加复杂度：**中**。需新增后端接口（update/delete）和前端跟进时间线
- 影响稳定性：**低**
- 更简单方案：先只加"跟进记录编辑/删除"和"我的跟进日历"，不加复杂统计

### 4. 逾期提醒机制硬编码且单一 `已修复(阈值可配置)`

逾期阈值硬编码为15天，不可配置。提醒只有站内通知，无邮件/短信。提醒类型单一（仅客户逾期跟进），缺少合同到期提醒、回款到期提醒。

- 必要：**是**。逾期是客户流失的主因
- 适合当前阶段：**部分**。提醒阈值可配置是刚需；邮件/短信集成可以后做
- 增加复杂度：**低**。阈值提取为系统配置项即可
- 影响稳定性：**无**
- 更简单方案：先在系统设置中加一个"逾期天数"配置字段，替代硬编码的15天

---

## P1 - 影响销售效率

### 5. 商机无详情页，无阶段变更记录 `已修复`

商机只有列表页和弹窗编辑，没有独立详情页。推进阶段时没有记录"谁在什么时候把阶段从X改到Y"，丢单没有记录原因。

- 必要：**是**。销售主管需要看阶段推进历史判断商机质量
- 适合当前阶段：**是**
- 增加复杂度：**中**。需新增商机详情页和阶段变更日志表
- 影响稳定性：**低**
- 更简单方案：先在商机列表点击行打开详情抽屉（类似线索），不单独建页面

### 6. 报价无状态流转，无转合同流程 `已修复`

报价状态只能直接修改字段，没有"草稿→已发送→已确认"的正式流转。报价确认后不能一键转为合同，需要手动重新录入。

- 必要：**是**。报价转合同是销售闭环的关键步骤
- 适合当前阶段：**是**
- 增加复杂度：**中**。需加状态流转按钮和"从报价生成合同"功能
- 影响稳定性：**低**
- 更简单方案：先只做"报价一键转合同"（复制数据到合同表），状态流转后续做

### 7. 合同详情用弹窗而非独立页面 `已修复`

合同只有列表+弹窗查看，弹窗内嵌套了回款计划和回款记录，信息密度极高，在弹窗中操作体验差。

- 必要：**是**。合同是最终成交凭证，信息量大
- 适合当前阶段：**是**
- 增加复杂度：**低**。新建一个 detail.vue 页面，已有后端 detail API
- 影响稳定性：**无**

### 8. 客户列表缺少日期范围筛选 `已修复`

客户列表支持按公司名、联系人、来源、等级筛选，但不支持按创建时间、最后跟进时间做范围筛选。无法回答"上周新增了哪些客户"这类常见问题。

- 必要：**是**。销售主管日常需要
- 适合当前阶段：**是**
- 增加复杂度：**低**。前端加日期选择器，后端加 WHERE 条件
- 影响稳定性：**无**

---

## P2 - 数据统计与管理

### 9. 报表无导出，无自定义日期范围 `已修复`

报表页面大部分统计无日期筛选，只有业绩排行支持。所有报表不能导出。

- 必要：**是**。主管需要导出报表做汇报
- 适合当前阶段：**部分**。报表导出优先级低于业务数据导出
- 增加复杂度：**中**
- 影响稳定性：**无**
- 更简单方案：先统一给所有报表加日期范围筛选，导出后续做

### 10. 销售目标/配额功能缺失 `已修复`

无销售目标设定，无目标达成率追踪。Dashboard 的业绩排名只有金额没有对比目标。

- 必要：**中**。有目标才能衡量绩效
- 适合当前阶段：**否**。当前阶段先把基础流程跑通
- 增加复杂度：**高**。需要目标表、目标分配、达成计算
- 影响稳定性：**低**

### 11. 售后工单硬删除 `已修复`

service.js 的删除接口使用 `DELETE FROM`，是全系统唯一硬删除的模块。关键业务数据丢失不可恢复。

- 必要：**是**。数据安全
- 适合当前阶段：**是**。属于bug修复级
- 增加复杂度：**低**。改为 `UPDATE SET deleted_at`
- 影响稳定性：**低**

---

# 当前低效率流程

| 问题 | 影响 | 优先级 |
|------|------|--------|
| 添加商机需要手动搜索客户，不能从客户详情直接创建 | 多3次点击 | P1 `已修复` |
| 添加合同需要手动搜索客户和商机，不能从商机一键转 | 多5次点击 | P1 `已修复` |
| 公海领取客户后没有快捷跟进入口 | 领取后需退回列表再进入详情 | P2 `已修复` |
| Dashboard 的"今日待跟进"点了跳转到客户列表而非客户详情 | 多一次点击 | P2 `已修复` |
| 商机列表的"推进阶段"弹窗需手动选择目标阶段 | 应自动显示下一阶段 | P2 `已修复` |

---

# 当前用户体验问题

| 问题 | 影响 | 优先级 |
|------|------|--------|
| 全系统无移动端适配 | 销售外出无法使用 | P2（大工程） |
| 电话号码正则硬编码中国手机格式 `^1[3-9]\d{9}$` | 外贸CRM无法录入国际号码 | P1 `已修复` |
| 表格列在窄屏溢出，无横向滚动 | 小屏幕用户无法正常使用 | P1 `已修复` |
| 没有列表列宽自适应或列显示控制 | 不同用户关注的字段不同 | P3 |

---

# 当前业务断层

| 断层 | 说明 | 优先级 |
|------|------|--------|
| 线索→商机无自动联动 | 线索转化成客户后，不会自动创建商机 | P1 `已修复` |
| 商机→报价无联动 | 商机推进到报价阶段，不能从商机直接创建报价 | P1 `已修复` |
| 报价→合同无联动 | 报价确认后不能转合同 | P1 `已修复` |
| 合同→回款有联动但无逾期预警 | 回款计划到期未回款无提醒 | P1 `已修复` |
| 客户详情的商机/合同Tab为空 | 销售无法在一个页面看完整客户画像 | P0 `已修复` |

---

# 当前不合理交互

| 问题 | 说明 | 优先级 |
|------|------|--------|
| 售后工单硬删除 | 全系统唯一，应改为软删除 | P1 `已修复` |
| 合同详情用弹窗 | 信息量大，弹窗操作体验差 | P1 `已修复` |
| `/contract/opportunity-list` 无权限过滤 | 任何用户能看到所有商机 | P1（安全）`已修复` |
| 设置页面是只读展示 | 有页面但不能配置任何东西 | P3 `部分修复(已加逾期天数配置)` |

---

# 建议新增功能

| 功能 | 必要性 | 阶段适合 | 复杂度 | 稳定性影响 | 更简单方案 | 优先级 |
|------|--------|----------|--------|------------|------------|--------|
| 客户详情Tab接入商机/合同 | 高 | 是 | 低 | 无 | 复用已有列表组件 | **P0** `已修复` |
| 客户/合同Excel导出 | 高 | 是 | 中 | 低 | 后端CSV流式导出 | **P0** `已修复(客户+合同)` |
| 跟进记录可编辑/删除 | 高 | 是 | 低 | 无 | — | **P0** `已修复` |
| 逾期天数可配置 | 高 | 是 | 低 | 无 | 系统设置加一个字段 | **P0** `已修复` |
| 商机详情抽屉 + 阶段变更日志 | 高 | 是 | 中 | 低 | 抽屉而非独立页 | **P1** `已修复` |
| 报价一键转合同 | 高 | 是 | 中 | 低 | 复制数据到合同表 | **P1** `已修复` |
| 合同独立详情页 | 高 | 是 | 低 | 无 | — | **P1** `已修复` |
| 客户列表日期范围筛选 | 高 | 是 | 低 | 无 | — | **P1** `已修复` |
| 电话号码支持国际格式 | 高 | 是 | 低 | 无 | 放宽正则 | **P1** `已修复` |
| 售后工单改为软删除 | 高 | 是 | 低 | 无 | — | **P1** `已修复` |
| 线索转化自动创建商机 | 中 | 是 | 低 | 无 | — | **P1** `已修复` |
| 回款到期提醒 | 中 | 是 | 中 | 低 | 复用现有提醒机制 | **P2** `已修复` |
| 报表统一日期筛选 | 中 | 部分 | 低 | 无 | — | **P2** `已修复` |
| 跟进日历视图 | 中 | 否 | 中 | 低 | 先做列表时间排序 | **P2** `已修复` |
| 报表导出 | 中 | 部分 | 中 | 低 | 后做，业务数据导出优先 | **P2** `已修复` |
| 销售目标/配额 | 中 | 否 | 高 | 低 | — | **P3** `已修复` |
| 移动端适配 | 高但非紧急 | 否 | 高 | 中 | 需专项规划 | **P3** |
| 客户去重/合并 | 中 | 否 | 高 | 中 | — | **P3** |
| 文件附件 | 低 | 否 | 高 | 低 | — | **P3** |

---

# 建议执行顺序

```
第一步：P0 共4项 — 已完成 4/4
  ✅ 客户详情Tab接入商机/合同
  ✅ 客户/合同Excel导出
  ✅ 跟进记录可编辑/删除
  ✅ 逾期天数可配置
第二步：P1 共8项 — 已完成 8/8
第三步：P2 共4项 — 已完成 4/4
第四步：P3 暂缓 — 等基础流程跑通后再规划
```

---

# 审查日期

2025-05-25
