# 铧旗CRM — MySQL → PostgreSQL 迁移对照手册

> 目标：Vercel（前端+后端）+ Supabase（PostgreSQL + Storage）部署
> 改造规模：34 个迁移文件 / ~47 张表 / ~536 条 SQL 查询 / 4 个定时任务

---

## 1. 数据类型转换

| MySQL | PostgreSQL | 说明 |
|-------|-----------|------|
| `INT AUTO_INCREMENT` | `SERIAL` 或 `INT GENERATED BY DEFAULT AS IDENTITY` | 自增主键 |
| `BIGINT AUTO_INCREMENT` | `BIGSERIAL` | 大自增主键 |
| `TINYINT` | `BOOLEAN`（布尔值）或 `SMALLINT`（数值） | 0/1 标志用 BOOLEAN |
| `DATETIME` | `TIMESTAMP` | 日期时间 |
| `DATETIME DEFAULT CURRENT_TIMESTAMP` | `TIMESTAMP DEFAULT NOW()` | 默认当前时间 |
| `DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | `TIMESTAMP DEFAULT NOW()` + 触发器 | PG 需要额外创建触发器 |
| `DATE` | `DATE` | ✅ 直接兼容 |
| `VARCHAR(n)` | `VARCHAR(n)` | ✅ 直接兼容 |
| `TEXT` | `TEXT` | ✅ 直接兼容 |
| `DECIMAL(p,s)` | `DECIMAL(p,s)` 或 `NUMERIC(p,s)` | ✅ 直接兼容 |
| `JSON` | `JSONB` | PG 推荐 JSONB（性能更好） |
| `ENUM('a','b','c')` | `VARCHAR(N) CHECK (col IN ('a','b','c'))` | PG 无原生 ENUM，用 CHECK 约束替代 |

### `ON UPDATE CURRENT_TIMESTAMP` 触发器模板

每个有 `update_time` 字段的表都需要创建：

```sql
CREATE OR REPLACE FUNCTION update_update_time()
RETURNS TRIGGER AS $$
BEGIN
    NEW.update_time = NOW();
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- 每张表都要执行（替换表名）
CREATE TRIGGER trg_<table>_update_time
    BEFORE UPDATE ON <table>
    FOR EACH ROW EXECUTE FUNCTION update_update_time();
```

---

## 2. DDL 语法转换

| MySQL | PostgreSQL | 说明 |
|-------|-----------|------|
| `ENGINE=InnoDB` | 删除 | PG 不需要 |
| `DEFAULT CHARSET=utf8mb4` | 删除 | PG 数据库级别设置编码 |
| `COLLATE=utf8mb4_unicode_ci` | 删除 | PG 不需要 |
| `COMMENT '...'`（表级） | `COMMENT ON TABLE <table> IS '...'` | 移到独立语句 |
| `COMMENT '...'`（列级） | `COMMENT ON COLUMN <table>.<col> IS '...'` | 移到独立语句 |
| `USE huakey_crm;` | 删除 | PG 通过连接字符串指定数据库 |
| `UNIQUE KEY name (col)` | `UNIQUE (col)` 或 `CONSTRAINT name UNIQUE (col)` | 语法不同 |
| `INDEX name (col)` | `CREATE INDEX name ON <table>(col)` | 一般写在 CREATE TABLE 外面 |
| `CREATE INDEX IF NOT EXISTS` | `CREATE INDEX CONCURRENTLY IF NOT EXISTS` | PG 需要额外注意 |

---

## 3. 常用函数转换

### 日期时间

| MySQL | PostgreSQL | 出现次数 |
|-------|-----------|---------|
| `NOW()` | `NOW()` | ~75 处 ✅ |
| `CURDATE()` | `CURRENT_DATE` | ~30 处 |
| `DATE(col)` | `col::date` | ~10 处 |
| `DATE_FORMAT(date, '%Y-%m')` | `TO_CHAR(date, 'YYYY-MM')` | ~30 处 |
| `DATE_FORMAT(date, '%Y-%m-%d')` | `TO_CHAR(date, 'YYYY-MM-DD')` | |
| `DATE_FORMAT(date, '%Y-%m-01')` | `DATE_TRUNC('month', date)::date` | ~15 处 |
| `DATE_SUB(NOW(), INTERVAL n DAY)` | `NOW() - INTERVAL 'n days'` | ~50 处 |
| `DATE_ADD(CURDATE(), INTERVAL n DAY)` | `CURRENT_DATE + INTERVAL 'n days'` | ~15 处 |
| `DATE_ADD(NOW(), INTERVAL n DAY)` | `NOW() + INTERVAL 'n days'` | |
| `DATEDIFF(NOW(), date)` | `EXTRACT(DAY FROM NOW() - date)` | ~25 处 |
| `DATEDIFF(date1, date2)` | `(date1 - date2)` 返回天数 | |
| `YEAR(col)` | `EXTRACT(YEAR FROM col)` | ~12 处 |
| `MONTH(col)` | `EXTRACT(MONTH FROM col)` | |
| `YEARWEEK(date, 1)` | `EXTRACT(YEAR FROM date) * 100 + EXTRACT(WEEK FROM date)` | ~2 处（语义不完全一致） |
| `TIMESTAMPDIFF(HOUR, a, b)` | `EXTRACT(EPOCH FROM b - a) / 3600` | ~1 处 |

### DATE_FORMAT 格式对照

| MySQL 格式符 | PostgreSQL 格式符 | 含义 |
|-------------|-------------------|------|
| `%Y` | `YYYY` | 四位年份 |
| `%y` | `YY` | 两位年份 |
| `%m` | `MM` | 月份（01-12） |
| `%d` | `DD` | 日期（01-31） |
| `%H` | `HH24` | 小时（00-23） |
| `%i` | `MI` | 分钟（00-59） |
| `%s` | `SS` | 秒（00-59） |
| `%M` | `Month` | 英文月份全名 |
| `%W` | `Day` | 英文星期全名 |

### 字符串

| MySQL | PostgreSQL | 出现次数 |
|-------|-----------|---------|
| `CONCAT(a, b)` | `a \|\| b` 或 `CONCAT(a, b)` | ✅ PG 也支持 CONCAT |
| `GROUP_CONCAT(col)` | `STRING_AGG(col::text, ',')` | ~1 处 |
| `NOT REGEXP` | `!~*`（不区分大小写） | ~1 处 |

### 其他

| MySQL | PostgreSQL | 出现次数 |
|-------|-----------|---------|
| `IFNULL(a, b)` | `COALESCE(a, b)` | ✅ PG 用 COALESCE |
| `COALESCE()` | `COALESCE()` | ~75 处 ✅ |
| `CASE WHEN` | `CASE WHEN` | ✅ 语法相同 |
| `IF(cond, a, b)` | `CASE WHEN cond THEN a ELSE b END` | 代码中无此用法 |

---

## 4. SQL 查询写法转换

### 占位符

```diff
- pool.query('SELECT * FROM crm_customer WHERE id = ?', [id])
+ pool.query('SELECT * FROM crm_customer WHERE id = $1', [id])

- pool.query('SELECT * FROM crm_customer WHERE name = ? AND status = ?', [name, status])
+ pool.query('SELECT * FROM crm_customer WHERE name = $1 AND status = $2', [name, status])
```

**注意：** PG 的 `pg` 驱动用 `$1, $2, $3...` 从 1 开始计数，不是从 0 开始。

### LIMIT / OFFSET

```diff
- LIMIT ?, ?                    -- MySQL 逗号语法 (offset, limit)
+ LIMIT $1 OFFSET $2            -- PG 关键字语法

- LIMIT ? OFFSET ?              -- 这种写法两边兼容
+ LIMIT $1 OFFSET $2            -- 只改占位符即可
```

### INSERT ... ON DUPLICATE KEY UPDATE

```diff
-- MySQL
- INSERT INTO crm_sales_target (...) VALUES (...)
- ON DUPLICATE KEY UPDATE target_amount = VALUES(target_amount)

-- PostgreSQL
+ INSERT INTO crm_sales_target (...) VALUES (...)
+ ON CONFLICT (user_id, year, month) DO UPDATE SET target_amount = EXCLUDED.target_amount
```

`VALUES(col)` → `EXCLUDED.col`，`ON CONFLICT` 需要指定唯一键列名。

### INSERT IGNORE

```diff
-- MySQL
- INSERT IGNORE INTO sys_validation_rule (...) VALUES (...)

-- PostgreSQL
+ INSERT INTO sys_validation_rule (...) VALUES (...)
+ ON CONFLICT DO NOTHING
```

### INSERT 后获取自增 ID

```diff
-- MySQL
- const [result] = await pool.query('INSERT INTO ...', [...]);
- const newId = result.insertId;

-- PostgreSQL (方式1: RETURNING)
+ const result = await pool.query('INSERT INTO ... VALUES (...) RETURNING id', [...]);
+ const newId = result.rows[0].id;

-- PostgreSQL (方式2: 序列)
+ const result = await pool.query("SELECT currval(pg_get_serial_sequence('table', 'id'))");
+ const newId = result.rows[0].currval;
```

### 幂等 DDL（ALTER TABLE ADD COLUMN）

```diff
-- MySQL（动态 SQL，非常常见）
- SET @col_exists = (SELECT COUNT(*) FROM information_schema.COLUMNS
-   WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'sys_role' AND COLUMN_NAME = 'view_all');
- SET @sql = IF(@col_exists = 0, 'ALTER TABLE sys_role ADD COLUMN view_all TINYINT DEFAULT 0', 'SELECT 1');
- PREPARE stmt FROM @sql;
- EXECUTE stmt;
- DEALLOCATE PREPARE stmt;

-- PostgreSQL
+ DO $$
+ BEGIN
+   IF NOT EXISTS (
+     SELECT 1 FROM information_schema.columns
+     WHERE table_name = 'sys_role' AND column_name = 'view_all'
+   ) THEN
+     ALTER TABLE sys_role ADD COLUMN view_all BOOLEAN DEFAULT FALSE;
+   END IF;
+ END $$;
```

### information_schema 查询

```diff
-- MySQL
- TABLE_SCHEMA = DATABASE()

-- PostgreSQL
+ table_schema = 'public'
```

---

## 5. 错误码转换

| MySQL 错误码 | PostgreSQL 错误码 | 含义 |
|-------------|------------------|------|
| `ER_DUP_ENTRY` | `23505`（unique_violation） | 唯一键冲突 |

```diff
- if (error.code === 'ER_DUP_ENTRY') {
+ if (error.code === '23505') {
```

---

## 6. 数据库驱动替换

### backend/config/database.js

```diff
- const mysql = require('mysql2/promise');
+ const { Pool } = require('pg');

- const pool = mysql.createPool({
-   host: process.env.DB_HOST || 'localhost',
-   port: parseInt(process.env.DB_PORT) || 3306,
-   user: process.env.DB_USER || 'root',
-   password: process.env.DB_PASSWORD || '',
-   database: process.env.DB_NAME || 'huakey_crm',
-   waitForConnections: true,
-   connectionLimit: 10,
-   queueLimit: 0,
-   enableKeepAlive: true,
-   keepAliveInitialDelay: 0
- });
+ const pool = new Pool({
+   connectionString: process.env.DATABASE_URL,
+   max: 10,
+   idleTimeoutMillis: 30000,
+   connectionTimeoutMillis: 2000,
+ });

- module.exports = pool;
+ module.exports = pool;
```

### backend/package.json

```diff
- "mysql2": "^3.22.3"
+ "pg": "^8.13.0"
```

---

## 7. 环境变量变更

```diff
# 移除
- DB_HOST=localhost
- DB_PORT=3306
- DB_USER=crm_user
- DB_PASSWORD=Huakey@2024
- DB_NAME=huakey_crm

# 新增
+ DATABASE_URL=postgresql://postgres:xxx@db.xxx.supabase.co:5432/postgres
+ SUPABASE_URL=https://xxx.supabase.co
+ SUPABASE_SERVICE_KEY=eyJ...
```

---

## 8. 文件上传改造

### multer 磁盘存储 → Supabase Storage

```diff
// backend/routes/upload.js

- const multer = require('multer');
- const path = require('path');
-
- const storage = multer.diskStorage({
-   destination: path.join(__dirname, '../uploads/attachments'),
-   filename: (req, file, cb) => {
-     const ext = path.extname(file.originalname);
-     const name = Date.now() + '-' + Math.random().toString(36).substring(2, 8) + ext;
-     cb(null, name);
-   }
- });
- const upload = multer({ storage, limits: { fileSize: 10 * 1024 * 1024 } });

+ const { createClient } = require('@supabase/supabase-js');
+ const multer = require('multer');
+
+ const storage = multer.memoryStorage();
+ const upload = multer({ storage, limits: { fileSize: 10 * 1024 * 1024 } });
+
+ const supabase = createClient(
+   process.env.SUPABASE_URL,
+   process.env.SUPABASE_SERVICE_KEY
+ );

// 上传改为 Supabase Storage
- const filePath = '/uploads/attachments/' + filename;
+ const { data, error } = await supabase.storage
+   .from('attachments')
+   .upload(filename, file.buffer, { contentType: file.mimetype });
+ const filePath = data.path;
```

### 删除本地文件服务

```diff
// backend/app.js
- const uploadsDir = path.join(__dirname, 'uploads');
- app.use('/uploads', express.static(uploadsDir));
+ // 文件由 Supabase Storage 直接提供，不需要本地静态服务
```

---

## 9. 定时任务改造

### node-cron → Vercel Cron Jobs

将 `backend/app.js` 中的 4 个 cron 任务抽取为独立 API 路由，然后在 `vercel.json` 中配置：

```json
{
  "crons": [
    { "path": "/api/cron/daily-scoring",    "schedule": "0 2 * * *" },
    { "path": "/api/cron/clean-logs",       "schedule": "0 3 * * *" },
    { "path": "/api/cron/auto-release",     "schedule": "0 1 * * *" },
    { "path": "/api/cron/generate-reminders","schedule": "30 8 * * *" }
  ]
}
```

新建 `backend/routes/cronJobs.js` 包含这 4 个端点，并在 `app.js` 中注册 `/api/cron/*` 路由。

---

## 10. Vercel 部署配置

### 根目录新建 `vercel.json`

```json
{
  "version": 2,
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm ci",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/backend/api/index" },
    { "source": "/((?!api).*)", "destination": "/index.html" }
  ]
}
```

### 新建 `backend/api/index.js`（Serverless 入口）

```js
const app = require('../app');
module.exports = app;
```

### 新建 `backend/vercel.json`

```json
{
  "version": 2,
  "functions": {
    "api/index.js": {
      "memory": 512,
      "maxDuration": 30
    }
  }
}
```

---

## 11. 改造文件清单

### 阶段 1：数据库（34 个文件）

全部在 `database/migrations/` 下，输出到 `supabase/migrations/`：
`001_init_baseline.sql` ~ `034_sales_target_table.sql`

### 阶段 2：后端（~50 个文件）

**关键文件（SQL 密集）：**
- `backend/config/database.js` — 驱动替换
- `backend/app.js` — 移除本地文件服务、移除 node-cron、注册 cron 路由
- `backend/routes/customer/detail.js` — 客户 CRUD（最多 SQL）
- `backend/routes/contract.js` — 合同+回款
- `backend/routes/report.js` — 报表（大量 DATE_FORMAT）
- `backend/routes/followUp.js` — 跟进记录
- `backend/routes/teamDashboard.js` — 仪表板（YEARWEEK）
- `backend/routes/ai.js` — AI 模块
- `backend/routes/reminder.js` — 提醒（大量日期函数）
- `backend/routes/target.js` — 销售目标（ON DUPLICATE KEY）
- `backend/routes/upload.js` — 文件上传（multer → Storage）
- `backend/routes/customer/import.js` — Excel 导入
- `backend/utils/scoring.js` — 评分（ON DUPLICATE KEY）
- `backend/utils/qualification-reminder.js` — 资质提醒
- `backend/scripts/generate_reminders.js` — 提醒生成
- `backend/scripts/auto_release.js` — 公海回收
- `backend/scripts/backup.js` — 备份（mysqldump → pg_dump）
- `backend/create_sys_log_table.js` — 建表 DDL

**新建文件：**
- `backend/api/index.js` — Serverless 入口
- `backend/routes/cronJobs.js` — Vercel Cron 端点

### 阶段 3：前端（~3 个文件）

- `frontend/vite.config.js` — 构建配置
- `frontend/src/utils/request.js` — API 基础路径
- `frontend/.env.production` — 生产环境变量

### 阶段 4：根目录

- `vercel.json` — Vercel 部署配置
- `.env.example` — 更新环境变量模板

---

## 12. 验证检查清单

- [ ] `supabase db push` 成功，所有 47 张表已创建
- [ ] 种子数据（admin 用户、权限、角色）已写入
- [ ] 本地 `vercel dev` 启动成功
- [ ] `POST /api/auth/login` 登录成功返回 JWT
- [ ] 客户列表/详情/增删改正常
- [ ] 合同+回款 CURD 正常
- [ ] 跟进记录正常
- [ ] 报表图表数据正常
- [ ] 文件上传到 Supabase Storage 并返回正确 URL
- [ ] 定时任务端点可独立调用（`/api/cron/*`）
- [ ] AI 模块（MiMo）正常
- [ ] RBAC 权限控制生效

---

> 此文档作为迁移全程的唯一语法对照源。遇到新的 MySQL 语法差异时，追加到对应章节。
