# Huakey CRM AI Development Guide

---

## 1. 项目背景

- **项目名称：** Huakey CRM
- **项目类型：** 企业级CRM系统（20-50人规模）
- **系统用途：** 销售管理、客户管理、商机跟进、报价、合同、售后、业务流程管理
- **核心定位：** 企业真实业务系统，非Demo/非玩具项目

### AI角色定位

AI不是"代码生成器"，必须同时扮演：

1. 企业级CRM产品经理
2. SaaS系统架构师
3. 高级全栈工程师
4. 数据库架构师
5. QA测试工程师
6. 企业流程顾问
7. UX交互设计师

---

## 2. 系统目标

### 开发目标

- 稳定优先
- 可维护优先
- 渐进式开发
- 企业真实业务优先
- 避免系统复杂化
- 保证长期可扩展
- 保证后续不变成屎山项目

### AI第一目标

不是"快速生成代码"，而是：

- 保证系统稳定
- 保证逻辑正确
- 保证权限安全
- 保证后续可维护
- 保证真实企业可用
- 保证后续可以持续迭代

### 最终目标

构建一个：稳定、清晰、企业真实可用、后期不崩、后期可维护、后期可扩展、业务员愿意使用的企业CRM系统。

**核心原则：稳定性优先级高于功能数量。**

---

## 3. 技术架构

### 架构目标

系统必须满足：

- 中小企业长期稳定运行
- 支持20-50人同时使用
- 易维护
- 易扩展
- 避免过度复杂化
- 避免后期成为屎山项目

系统优先考虑：
- 稳定性
- 可维护性
- 开发效率
- 实际业务可用性

而不是：
- 炫技
- 微服务复杂架构
- 过度抽象
- 过度设计

### 推荐架构原则

采用前后端分离架构。

推荐结构：

**Frontend：**
- Vue3 / React
- TypeScript
- Vite
- Element Plus / Ant Design

**Backend：**
- Node.js / Java Spring Boot
- RESTful API

**Database：**
- MySQL

**Cache：**
- Redis（后期按需增加）

**File Storage：**
- 本地存储或OSS

### 架构规则

**禁止：**

- 微服务化
- 分布式复杂架构
- 多数据库混用
- 无意义中间件
- 复杂消息队列
- 无意义设计模式

**系统必须保持：**

- 单体架构优先
- 模块化开发
- 低耦合
- 高可维护性

### 模块划分原则

每个模块必须：

- 职责单一
- 可独立维护
- 不允许循环依赖
- 不允许跨模块乱调用

**推荐模块：**

- 客户模块
- 联系人模块
- 跟进模块
- 商机模块
- 报价模块
- 合同模块
- 售后模块
- 用户权限模块
- 数据统计模块
- 系统设置模块

### 技术决策原则

新增技术前必须分析：

1. 是否真的必要
2. 是否增加维护成本
3. 是否增加学习成本
4. 是否适合中小团队
5. 是否会导致后期复杂化

**禁止为了"高级感"引入技术。**

---

## 4. 开发原则

- 稳定性优先级高于功能数量
- 小步迭代，一次只改一个点
- 先分析，后动手，等确认
- 最小修改方案，不做多余改动
- 每一步都必须可回滚
- 每一步都必须可测试
- 每一步都必须保证系统可运行
- 不追求"高级感"，追求真实可用
- 不引入复杂依赖
- 不生成高耦合代码
- 不生成重复代码
- 不出现硬编码
- 不出现命名混乱

---

## 5. 严禁事项

1. 不允许擅自重构整个系统
2. 不允许擅自修改系统架构
3. 不允许修改无关代码
4. 不允许破坏已有功能
5. 不允许一次性大范围修改
6. 不允许为了"高级感"增加复杂度
7. 不允许过度设计
8. 不允许生成无法维护的代码
9. 不允许引入复杂依赖
10. 不允许擅自更换技术栈
11. 不允许擅自修改数据库核心结构
12. 不允许删除旧字段
13. 不允许破坏旧数据兼容
14. 不允许忽略权限系统
15. 不允许仅做前端假权限
16. 不允许生成不完整方案
17. 不允许跳过分析直接输出代码
18. 不允许盲目猜测问题原因
19. 不允许修改未分析的文件
20. 不允许一次修改多个核心模块
21. 不允许生成高耦合代码
22. 不允许生成重复代码
23. 不允许出现硬编码
24. 不允许出现命名混乱
25. 不允许省略风险分析
26. 不允许省略数据库影响分析
27. 不允许省略权限影响分析
28. 不允许省略接口影响分析
29. 不允许省略回滚方案
30. 不允许为了修一个bug破坏整体系统
31. 不允许修改公共utils导致全局影响
32. 不允许大规模搜索替换
33. 不允许自动升级依赖版本

---

## 6. 开发流程

在输出任何代码前，必须严格执行以下步骤：

| 步骤 | 内容 |
|------|------|
| 第一步 | 阅读相关文件 |
| 第二步 | 分析模块结构 |
| 第三步 | 分析调用关系 |
| 第四步 | 分析数据库结构 |
| 第五步 | 分析接口关系 |
| 第六步 | 分析权限逻辑 |
| 第七步 | 分析前端影响 |
| 第八步 | 分析后端影响 |
| 第九步 | 分析移动端影响 |
| 第十步 | 分析性能影响 |
| 第十一步 | 分析未来扩展性 |
| 第十二步 | 列出风险点 |
| 第十三步 | 给出最小修改方案 |
| 第十四步 | 等待确认 |

**未经确认：禁止输出正式修改代码。**

### 小步迭代开发模式

1. 一次只开发一个模块
2. 一次只解决一个问题
3. 一次只修改一个核心逻辑
4. 一次只做一个数据库改动
5. 每一步都必须可回滚
6. 每一步都必须可测试
7. 每一步都必须保证系统可运行

**禁止：**

1. 一次性生成整个系统
2. 一次性修改多个核心模块
3. 未测试继续下一步
4. 功能堆叠开发

---

## 7. 数据库规范

### 必须

1. 保证兼容旧数据
2. 保证旧功能正常运行
3. 提供完整SQL
4. 提供字段说明
5. 提供索引方案
6. 分析数据量增长风险
7. 分析查询性能
8. 分析未来扩展性
9. 分析关联表影响
10. 分析回滚方案

### 禁止

1. 删除生产字段
2. 修改核心主键逻辑
3. 随意改变字段类型
4. 破坏关联关系
5. 生成危险SQL
6. 不加索引
7. 不分析性能

---

## 8. 权限系统规范

### 必须考虑

1. 角色权限
2. 数据权限
3. 部门隔离
4. 操作日志
5. 越权风险
6. API权限校验
7. 页面权限控制
8. 按钮权限
9. 菜单权限
10. 数据可见范围

### 禁止

1. 默认全部数据可见
2. 仅做前端权限
3. 通过隐藏按钮假权限
4. 不做接口校验
5. 不记录关键操作日志

---

## 9. UI/UX规范

### 必须优先考虑

1. 销售录入效率
2. 减少点击次数
3. 减少页面跳转
4. 移动端操作体验
5. 快捷操作
6. 批量操作
7. 搜索筛选
8. 表单填写效率
9. 信息层级清晰
10. 真实业务场景
11. 表格查看效率
12. 跟进效率
13. 减少学习成本

### 禁止

1. 华而不实UI
2. 复杂操作流程
3. 深层级菜单
4. 低效率交互
5. 大量弹窗
6. 不适合业务员使用

---

## 10. Bug修复规范

### 禁止直接修改代码。

### 必须

1. 先分析根因
2. 列出所有可能原因
3. 给出最可能原因
4. 给出排查方案
5. 给出影响范围
6. 给出最小修复方案
7. 给出风险分析
8. 给出回滚方案
9. 等待确认

### 禁止

1. 盲目猜测
2. 大范围重构
3. 顺手修改其他功能
4. 一次修多个无关问题

---

## 11. 代码规范

### 必须检查

1. 是否存在重复代码
2. 是否存在文件过大
3. 是否存在逻辑耦合
4. 是否存在硬编码
5. 是否存在命名混乱
6. 是否存在重复接口
7. 是否存在重复SQL
8. 是否存在状态混乱
9. 是否存在不可维护代码
10. 是否存在潜在性能问题

### 必须

1. 保持代码结构清晰
2. 添加必要注释
3. 保持命名统一
4. 保持模块职责单一
5. 保持后续可扩展

---

## 12. 文件结构规范

### 文件结构目标

必须保证：

- 文件结构清晰
- 模块职责明确
- 后期易维护
- AI容易理解项目结构
- 新开发人员容易接手

### 前端目录规范

推荐：

```
src/
├── api/
├── views/
├── components/
├── layouts/
├── router/
├── store/
├── hooks/
├── utils/
├── styles/
├── constants/
├── permission/
```

### 后端目录规范

推荐：

```
server/
├── controllers/
├── services/
├── models/
├── routes/
├── middleware/
├── utils/
├── config/
├── permissions/
├── logs/
```

### 文件命名规则

**必须：**

- 使用统一命名风格
- 文件名必须语义化
- 禁止拼音
- 禁止无意义缩写
- 禁止 random 命名

**推荐：**

- customer.service.ts
- customer.controller.ts
- customer.api.ts

**禁止：**

- a.js
- test2.js
- newFile.js

### 文件大小限制

**建议：**

- 单文件不超过500行
- 超过必须拆分模块

**禁止：**

- 超大文件
- 所有逻辑堆一个文件
- 页面和业务逻辑混写

### 模块职责规范

- **Controller：** 只负责请求处理
- **Service：** 只负责业务逻辑
- **Model：** 只负责数据结构

**禁止：**

- Controller写复杂业务
- 页面直接操作数据库
- 跨层乱调用

---

## 13. API规范

### API设计目标

API必须：

- 清晰
- 统一
- 易维护
- 易扩展
- 权限安全

### API命名规范

**必须：**

- 使用RESTful风格
- 使用英文
- 使用复数资源名

**例如：**

- GET /api/customers
- POST /api/customers
- PUT /api/customers/:id
- DELETE /api/customers/:id

**禁止：**

- /getCustomer
- /customerListData

### 返回格式统一

必须统一：

```json
{
  code: 200,
  message: "success",
  data: {}
}
```

**禁止：**

- 不统一返回结构
- 有些接口返回字符串
- 有些接口返回数组

### 权限规范

所有接口必须：

- 登录校验
- 权限校验
- 数据权限校验

**禁止：**

- 无权限接口直接开放
- 仅前端限制

### 错误处理规范

**必须：**

- 返回明确错误信息
- 禁止返回系统堆栈
- 记录错误日志

### 接口版本规范

后期如需升级使用：

- /api/v1/
- /api/v2/

**禁止直接破坏旧接口。**

---

## 14. 命名规范

### 命名目标

所有命名必须：

- 统一
- 可读
- 可维护
- 语义明确

**禁止：**

- 拼音
- 随意缩写
- 无意义命名

### 数据库命名

**表名：**

必须小写+下划线。

例如：

- crm_customer
- crm_contact

**字段：**

例如：

- customer_name
- created_at

**禁止：**

- CustomerTable
- tb1
- name1

### 前端命名

**组件（PascalCase）：**

例如：CustomerTable.vue

**变量（camelCase）：**

例如：customerList

**常量（UPPER_CASE）：**

例如：DEFAULT_PAGE_SIZE

### API命名

例如：

- customerApi
- contractApi

**禁止：**

- api2
- requestData

### 权限命名

必须统一格式：

- customer:view
- customer:create
- customer:update
- customer:delete

---

## 15. 输出格式规范

每次输出必须包含：

1. 问题分析
2. 根因分析
3. 风险分析
4. 修改方案
5. 数据库影响
6. 权限影响
7. 接口影响
8. 前端影响
9. 后端影响
10. 移动端影响
11. 性能影响
12. 测试建议
13. 回滚方案
14. 修改文件列表

**没有完整分析：禁止直接输出代码。**

---

## 16. 测试规范

### 测试目标

所有功能必须保证：

- 不影响旧功能
- 不影响权限系统
- 不影响数据库
- 不影响移动端

### 开发后必须测试

1. 功能是否正常
2. 页面是否报错
3. 权限是否正常
4. 数据是否正确
5. 是否影响旧模块
6. 是否存在性能问题
7. 移动端是否正常

### Bug修复测试

修复后必须：

- 复测原问题
- 检查关联功能
- 检查权限
- 检查接口
- 检查数据库

**禁止：未测试直接提交。**

### 测试优先级

优先测试：

1. 权限
2. 数据安全
3. 客户数据
4. 删除操作
5. 批量操作

---

## 17. 回滚规范

### 回滚目标

所有修改必须支持：

- 快速回滚
- 数据恢复
- 功能恢复

**禁止：无法回退的危险修改。**

### 修改前必须

1. 备份数据库
2. 记录修改文件
3. 记录修改内容
4. 记录影响模块

### 数据库修改

必须：

- 提供回滚SQL
- 禁止危险删除
- 禁止不可恢复修改

### 代码回滚

必须：

- 每次修改独立提交
- 保持小步提交
- 禁止一次性大范围提交

### Bug回滚

如果修改导致以下情况，必须立即回滚：

- 系统异常
- 权限异常
- 数据异常

---

## 18. CRM业务规则

### CRM核心目标

系统必须真正服务企业业务。

**优先考虑：**

- 销售效率
- 客户跟进效率
- 数据安全
- 流程清晰
- 使用简单

**禁止：**

- 华而不实
- 复杂流程
- 无意义功能

### 客户规则

客户必须支持：

- 客户标签
- 客户状态
- 客户来源
- 客户等级
- 客户负责人
- 客户公海
- 客户去重

**禁止重复客户数据。**

### 跟进规则

必须支持：

- 跟进记录
- 跟进提醒
- 下次跟进时间
- 跟进状态
- 跟进历史

**禁止客户长期无人跟进。**

### 商机规则

必须支持：

- 商机阶段
- 商机金额
- 成交概率
- 丢单原因

**必须支持销售漏斗。**

### 权限规则

- **销售：** 只能查看自己的客户
- **主管：** 可以查看部门数据
- **管理员：** 拥有完整权限

### 删除规则

关键数据禁止真实删除，必须：

- 软删除
- 保留日志

### 日志规则

关键操作必须记录日志：

- 删除
- 修改
- 分配客户
- 权限修改
- 合同修改

### 数据统计规则

必须支持：

- 销售统计
- 客户统计
- 跟进统计
- 成交统计

### 移动端规则

核心功能必须适配移动端：

- 客户查看
- 跟进记录
- 跟进提醒
- 联系客户

---

## 19. 核心模块保护

以下模块默认禁止修改：

- auth
- permission
- middleware
- request
- axios
- RBAC
- customer核心逻辑

除非明确允许。

---

## 20. 安全规则

严格禁止：

- 输出真实.env内容
- 输出数据库密码
- 输出JWT_SECRET
- 输出API密钥

AI生成SQL时：

只允许SELECT。

禁止：

- DELETE
- UPDATE
- DROP
- ALTER
- INSERT

---

## 21. Docker规则

禁止删除：

- volumes
- uploads映射
- mysql数据卷

---

## 22. Git规则

每次修复后建议：

git commit

格式：

fix(module): 描述

例如：

fix(auth): 修复token失效问题

---

## 23. 当前阶段

当前阶段：

稳定化 > 安全 > 测试 > 性能 > 新功能

AI禁止主动新增复杂功能。

---

## 24. 文件上传规范

### 必须

1. 限制文件大小（当前统一10MB）
2. 使用文件类型白名单（扩展名 + MIME双重校验）
3. 重命名上传文件（时间戳-随机串.ext），禁止使用原始文件名
4. 上传接口必须JWT认证
5. 删除附件必须校验当前用户权限（owner_id或管理员）
6. 导入完成后自动删除临时文件
7. multer错误必须捕获并返回友好提示

### 禁止

1. 允许上传可执行文件（.exe/.sh/.bat/.js等）
2. 使用原始文件名存储
3. 上传接口无需认证
4. 任何人可删除他人附件

---

## 25. 数据导入规范

### 必须

1. 导入前提供预览 + 确认两步流程
2. 中文列名自动映射到数据库字段
3. 导入数据必须清洗（去空格、标准化电话邮箱）
4. 导入数据必须校验（required/format/range）
5. 批量去重（与数据库已有数据对比）
6. 超长字段必须truncate保护
7. 使用数据库事务，失败整体回滚
8. 导入接口必须校验权限

### 禁止

1. 无预览直接导入
2. 无校验直接写入数据库
3. 导入失败不回滚
4. 导入大数据量时阻塞主线程

---

## 26. 日志脱敏规范

### 必须

1. 日志params字段禁止记录明文密码
2. 日志params字段禁止记录明文手机号、邮箱、身份证号、银行卡号
3. 日志中间件记录req.body前必须经过脱敏处理
4. 脱敏规则：手机号保留前3后4位，邮箱用户名脱敏，密码替换为******
5. 利用现有 utils/mask.js 的脱敏函数

### 禁止

1. sys_log表中出现明文密码
2. sys_log表中出现完整手机号/邮箱
3. 日志中间件直接记录原始req.body不做处理
4. 脱敏工具存在但不使用

### 关键风险

当前日志中间件（middleware/logger.js）直接记录 `req.body`，登录密码、客户手机号、邮箱等敏感信息以明文存入sys_log表。脱敏工具（utils/mask.js）已存在但未接入日志系统。

---

## 27. 定时任务规范

### 必须

1. 定时任务必须记录执行日志（开始/结束/耗时/错误）
2. 关键任务必须有执行状态检查
3. 定时任务代码必须有try-catch包裹
4. 定时任务错误不能导致主进程崩溃
5. 独立脚本必须有使用说明和调度配置

### 禁止

1. 定时任务中执行危险SQL（DROP/TRUNCATE/DELETE无WHERE）
2. 定时任务无错误处理
3. 定时任务执行结果不记录日志
4. 定时任务修改数据不使用事务

---

## 28. 数据库迁移规范

### 必须

1. 迁移文件命名统一：NNN_描述.sql（如031_add_xxx.sql）
2. 每个迁移文件只做一件事
3. 迁移SQL必须可重复执行（使用IF NOT EXISTS等）
4. 新增字段必须指定默认值，不能破坏旧数据
5. 迁移前必须在开发环境验证通过

### 禁止

1. 直接修改已执行的迁移文件
2. 迁移文件中包含DELETE/TRUNCATE危险操作
3. 迁移文件中修改或删除已有字段
4. 跳过编号直接执行后面的迁移
5. 无迁移直接手动ALTER TABLE

---

## 29. 环境变量规范

### 必须

1. 所有敏感配置必须通过环境变量注入（数据库密码、JWT密钥、API密钥）
2. .env.example必须包含所有环境变量并有注释说明
3. 生产环境必须设置JWT_SECRET且不能使用默认值
4. 环境变量在代码中通过 process.env 读取，禁止硬编码

### 禁止

1. 代码中硬编码数据库密码
2. 代码中硬编码JWT密钥
3. .env文件提交到Git仓库
4. 生产环境使用默认密码/密钥
5. 缺少.env.example导致新开发者无法配置

---

## 30. 接口入参校验规范

### 必须

1. 所有写操作接口（POST/PUT/DELETE）必须使用Joi校验req.body
2. 关键查询接口必须使用queryValidate校验req.query
3. Joi schema必须设置 stripUnknown: true 过滤未知字段
4. Joi校验必须 abortEarly: false 收集所有错误
5. 校验失败返回明确的字段级错误信息

### 当前缺口

以下路由文件缺少Joi校验，直接接收req.body：

- dept.js（部门管理）
- role.js（角色管理）
- product.js（产品管理）
- followUp.js（跟进记录）
- service.js（售后服务）

### 禁止

1. 写操作接口不校验直接使用req.body
2. 信任前端传入的id/user_id等字段不做类型校验
3. 校验失败返回500而非400
4. 校验规则与数据库字段约束不一致

---

## 31. 数据导出安全规范

### 必须

1. 导出接口必须有数量上限（建议每次最多5000条）
2. 导出接口必须校验权限（authenticateToken + checkPermission）
3. 导出接口必须校验数据权限范围（防止越权查看他人数据）
4. 导出操作必须记录审计日志
5. 导出的敏感字段（手机号、邮箱）根据角色决定是否脱敏

### 禁止

1. 导出接口无LIMIT保护，可导出全量数据
2. 导出接口缺少数据权限过滤
3. 导出操作不记录日志
4. 非管理员导出完整手机号/邮箱

---

## 32. 分页规范

### 必须

1. 所有列表接口必须校验pageSize上限（max 200）
2. 使用Joi校验：`Joi.number().integer().min(1).max(200)`
3. 前端page-sizes选项不得超过后端上限

### 禁止

1. 列表接口不校验pageSize，攻击者可传pageSize=999999
2. 前端硬编码超过后端上限的pageSize值

### 当前缺口

以下路由缺少pageSize上限校验：

- user.js（用户列表）
- log.js（操作日志）
- report.js（报表）
- contract.js（部分列表端点）

---

## 33. 敏感数据接口规范

### 必须

1. API响应中禁止返回password字段（即使哈希值也不行）
2. 用户列表接口对非管理员隐藏其他用户的phone/email
3. 利用现有utils/mask.js进行字段脱敏
4. 邮箱/手机号等PII字段根据调用方角色决定完整展示或脱敏展示

### 禁止

1. 任何接口响应中包含password字段
2. 用户列表对所有角色暴露完整手机号/邮箱
3. 脱敏工具存在但不使用

---

## 34. 数据删除级联规范

### 必须

1. 软删除主记录时，必须级联处理关联数据（软删除/标记）
2. 删除前必须检查是否存在不可丢失的关联数据
3. 部门删除必须改为软删除，并检查部门下是否仍有用户
4. 用户删除（逻辑删除）后，其创建的客户/合同/跟进记录必须保持可查询

### 禁止

1. 物理删除有外键关联的记录（如sys_dept）
2. 删除主记录后关联数据变成"幽灵数据"（可查但无归属）
3. 删除操作不检查关联数据直接执行
