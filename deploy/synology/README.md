# ⚠️ 注意

本目录仅保留运维辅助脚本。Docker 部署配置的唯一事实来源是仓库根目录：

- `../../docker-compose.synology.yml` — Docker Compose 部署配置
- `../../Dockerfile.synology` — 应用镜像构建文件

**禁止在本目录创建独立的 compose/Dockerfile 副本**，避免版本漂移导致生产部署失败。
