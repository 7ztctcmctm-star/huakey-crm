{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "grep -rn 'EXTRACT\\|INTERVAL.*day\\|DELIMITER\\|\\$\\$' $CLAUDE_FILE_PATH 2>/dev/null && echo '⚠️ PostgreSQL syntax detected in MySQL project!' || true"
          }
        ]
      }
    ]
  }
}
