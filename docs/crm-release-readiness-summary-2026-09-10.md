# 发布就绪总览（Core v1）

> **日期**：2026-09-10
> **负责人**：David
> **作用**：把分散在各专项文档中的结论收拢为**放行判定入口**
> **原则**：本文所有数字均为**实测值**，未采信任何既有文档的声明

---

## 一、放行判定

> **建议：不阻塞核心功能发布，但建议先处理 2 项高优先级项。**

| 层级 | 状态 |
|------|------|
| 功能链路（Customer → Opportunity → Quote → Contract） | ✅ 测试全绿 |
| 前端构建与单元测试 | ✅ 通过 |
| 后端 lint 与单元测试 | ✅ 通过 |
| 上线阻塞项（P0/P1 共 15 项） | ⚠️ 13 项属实，**2 项存在缺陷** |
| 移动端适配 | ⚠️ 自动化已通过，**真机未验证** |
| 跨浏览器兼容 | ❓ **无有效证据**（本地不可信、CI 从未运行过） |
| 灾备能力 | ⚠️ 备份可还原，但**端到端恢复未演练** |

---

## 二、测试实测结果

| 层 | 命令 | 结果 |
|----|------|------|
| 后端单元 | `cd backend && npm test` | ✅ **106 套件 / 1034 用例全部通过** |
| 后端 lint | `cd backend && npm run lint` | ✅ **0 errors**（5 warning 全来自未入库的本地 `backend/tmp/`） |
| 后端覆盖率 | 同上 | ✅ 48.56% / 30.83% / 46.88% / 51.64%，**阈值（40/30/40/40）全部达标** |
| 前端单元 | `cd frontend && npm run test` | ✅ **11 文件 / 44 用例全部通过** |
| 前端构建 | `npm run build -- --emptyOutDir=false` | ✅ 通过 |
| E2E（chromium） | `E2E_USE_MIGRATIONS=true npm run test:e2e -- --project=chromium` | ✅ **34 passed / 0 skipped / 0 failed** |

> **E2E 数字说明**：收敛前为 45 passed / 3 skipped。差值为 `cross-browser.spec.js` 重构所移除的
> 重复用例（每个 project 从 16 个收敛为 2 个）。被移除的 3 个 skip 是「登录页不显示侧边栏」
> 这类**恒真断言**（登录页本就无侧边栏），非有效覆盖。

---

## 三、待处理事项（按优先级）

### 高优先级

| 编号 | 事项 | 依据 |
|------|------|------|
| ~~**H-1**~~ **✅ 已修复** | `deploy.sh` 的 P0-3（权限码初始化）与 P0-5（迁移结果校验）原为「仅告警、不中止」。已改为**累积失败 → 流程跑完 → 末尾以非零码退出并醒目汇总**（不做中途 abort，避免留下半成品状态） | 核验记录 §三-1、§三-2 |
| **H-2** | **跨浏览器兼容无有效证据**。`cross-browser-test` 作业此前因触发器缺失从未运行；修复后仍**一次都没跑过**。建议手动触发 `workflow_dispatch` 取得首份结果 | E2E 诊断报告 §十 |

### 中优先级

| 编号 | 事项 | 依据 |
|------|------|------|
| ~~M-1~~ **✅ 已修复** | P1-10：`docker-compose.synology.yml` 原**无任何 CPU 限制**。已按文档原意补齐 5 个服务（1.5/0.5/1.0/0.5/0.5）。**残留风险**：本机无 Docker，未能 `docker compose config` 语义校验，首次部署前请在 NAS 上验证 | 核验记录 §三-4 |
| ~~M-2~~ **✅ 已修复** | B-01：备份校验原仅检查**文件非空**。已追加 `gzip -t` 完整性校验，损坏则 `exit 1` | 核验记录 §6.3 |
| M-3 | 移动端**真机走查 5 项**未执行（触摸手势、真机字体、iOS 安全区等） | 移动端审查 §七点五 |
| M-4 | 灾备**端到端恢复**未演练，RTO / RPO 未实测 | 核验记录 §6.3 B-02 |

### 低优先级

| 编号 | 事项 | 依据 |
|------|------|------|
| L-1 | 前端**无覆盖率配置与阈值**（后端有硬阈值，前端完全没有）。建议补充 | 本轮实测 |
| L-2 | 图标库规范条目与实际技术栈冲突（规范写 Lucide/Heroicons，实际 78 处全为 Element Plus），建议修正规范 | 优化规范 §10.4 |
| L-3 | `docs/crm-ui-cleanup-report.md` 等文档存在过时数字（如 console 处数、迁移版本号） | 多份文档 |

---

## 四、本次会话产出

### 提交（13 个）

| Commit | 内容 |
|--------|------|
| `a873ed6` | 补做灾备演练 |
| `89a774e` | 补齐 P1 剩余 6 项核验 |
| `df817e2` | 上线前阻塞项核验记录 |
| `57c8a7c` | 修复 `cross-browser-test` 死配置 |
| `6503f29` | 重构 `cross-browser.spec.js` + 纠正误判 |
| `0bc7f44` | 打通 E2E + 修复其暴露的真实缺陷 |
| `e064099` | 移动端适配：侧边栏抽屉化 + 断点补齐 |
| `c923960` | `el-empty` 全量迁移 + 组件缺陷修复 |
| `bb0d0a4` | 日志收敛到统一出口 |
| `c5a65a4` | 统一空状态 + 四大核心表格 |
| `ab29575` | 表格骨架屏 + 四大核心列表 |
| `a79f04d` | 图表色板 token 化 + 文档对齐 |
| `0c750f7` | checkpoint（保住 109 个裸奔改动） |

### 专项文档

| 文档 | 内容 |
|------|------|
| `crm-e2e-diagnosis-and-responsive-fix.md` | E2E 根因诊断、响应式修复、CI 死配置、自我纠错记录 |
| `crm-deployment-blockers-verification-2026-09-10.md` | P0/P1 全 15 项实测、灾备演练 |
| `crm-mobile-responsive-audit.md` | 移动端 9 项问题与人工走查清单 |
| `crm-ui-visual-optimization-spec.md` | 三阶段落地状态更新 |

---

## 五、本次会话修复的真实缺陷汇总

按「只有实测才能发现」排序：

| # | 缺陷 | 类型 |
|---|------|------|
| 1 | 顶栏 375px 横向溢出 **57px**（实测 432 > 375） | 响应式 |
| 2 | 统计卡片平板断点错误，1024px 仍 4 列 | 响应式 |
| 3 | `cross-browser.spec.js` 4 倍重复执行 + 用例错标 + 过宽选择器 | 测试设计 |
| 4 | `responsive.spec.js` 全部用例只访问 `/login`，断言恒真（**虚假信心**） | 测试设计 |
| 5 | `cross-browser-test` CI 作业**从未执行**（触发器缺失） | CI 配置 |
| 6 | `quotation-to-contract.spec.js` 陈旧断言（操作列已收敛进下拉） | 测试维护 |
| 7 | E2E 数据库初始化死局（001 基线标记 + 默认清库） | 测试基础设施 |
| 8 | `EmptyState.title` 非响应式 / 缺 `compact` 变体 / 插槽无容器 | 组件缺陷 |
| 9 | 移动端基本没有适配（侧边栏固定 230px，手机内容区仅剩 160px） | 响应式 |

---

## 六、本总览的局限

1. **跨浏览器兼容无结论**——本机沙箱会污染多项目运行，须以 CI 结果为准；
2. **真机渲染未验证**——布局层已由自动化守住，触摸/字体/安全区未测；
3. **生产数据量未验证**——所有测试均基于测试库；
4. **灾备未端到端演练**——仅验证了备份产物可还原。

> 上述四项不因本次核验而消除，放行前应逐项确认或由负责人明确接受风险。

---

*总览由 David 出具 · 2026-09-10 · 全部数字为实测值*
